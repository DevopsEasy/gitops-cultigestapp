apiVersion: apps/v1
kind: Deployment
metadata:
  name: cultigestapp:1.0.0-10
spec:
  replicas: 1
  selector:
    matchLabels:
      run: cultigestapp:1.0.0-10
  template:
    metadata:
      labels:
        run: cultigestapp:1.0.0-10
    spec:
      containers:
      - name: cultigestapp:1.0.0-10
        image: devopseasy/cultigestapp:1.0.0-10
        ports:
          - containerPort: 80
        imagePullPolicy: IfNotPresent
