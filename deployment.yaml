apiVersion: apps/v1
kind: Deployment
metadata:
  name: cultigestapp:1.0.0-9
spec:
  replicas: 1
  selector:
    matchLabels:
      run: cultigestapp:1.0.0-9
  template:
    metadata:
      labels:
        run: cultigestapp:1.0.0-9
    spec:
      containers:
      - name: cultigestapp:1.0.0-9
        image: devopseasy/cultigestapp:1.0.0-9
        ports:
          - containerPort: 80
        imagePullPolicy: IfNotPresent
